#include <stdio.h>
#include "main.h"
/**
 * _strlen - check string lenth
 * 
 * @length: length of string
 * Return: int
 */

int _strlen(char *s)
{
int length = 0;
length = strlen(*s);
  return(length);
}
