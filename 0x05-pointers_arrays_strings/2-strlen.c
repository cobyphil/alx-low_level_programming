#include <stdio.h>
#include "main.h"
/**
 * _strlen - check string lenth
 * 
 * @length: length of string
 * Return: int
 */

int _strlen(char *s)
{
char n;
char length;
n = *s
length = strlen(n);
  return(length);
}
